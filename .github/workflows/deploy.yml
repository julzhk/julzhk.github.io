name: Deploy to GitHub Pages
on:
  push:
    branches: ["main"]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: julzhk/gh-pages-pelican-action
      env:
        GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
